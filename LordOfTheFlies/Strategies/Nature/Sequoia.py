import random as r
import numpy as n

def strategy(tile, nearx, neary, types, energies, yourenergy, memory):
    if r.random() < 0.02:
        return n.array([0,0]), False, -1, [], [], [[100, 110, 20], "cx.append((mx[x] + r.randint(-2,2)) % mapx)\ncy.append((my[x] + r.randint(-2,2)) % mapy)\nce.append(energ[2])\ncm.append([])\ncc.append(2)\nccol.append([200,10,10])"]
    else:
        return n.array([0,0]), False, -1, [], [], [[100, 110, 20], ""]
